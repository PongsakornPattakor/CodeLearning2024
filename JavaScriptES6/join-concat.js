// Join
const data = [100, 200, 300];
console.log(data);

const result = data.join("/"); // คั่นแต่ละค่าด้วย /
console.log(result);

// Concat
const data1 = [10, 30, 50];
const data2 = [20, 40];

const allData = data1.concat(data2);
console.log(allData);
